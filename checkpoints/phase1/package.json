{
  "name": "kraken-agent",
  "version": "1.0.0",
  "description": "Kraken Agent - Self-contained orchestrator with 3 async clusters, embedded Shark/Manta agents, and Kraken-Hive Mind. Drop-in OpenCode plugin.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --bundle --external @opencode-ai/plugin",
    "build:all": "bun run build && bun run build:shark",
    "build:shark": "bun build shark-agent/src/index.ts --outdir shark-agent/dist --target bun --format esm --bundle --external @opencode-ai/plugin",
    "typecheck": "bun tsc --noEmit",
    "test": "bun test",
    "prepack": "bun run build"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.3.6"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.3.6",
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "opencode",
    "plugin",
    "agent",
    "multi-agent",
    "orchestrator",
    "shark",
    "manta",
    "kraken",
    "hive-mind",
    "cluster"
  ],
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.0.0"
  }
}
