{
  "name": "kraken-agent",
  "version": "1.4.0",
  "description": "Kraken Agent v1.4 — Runtime-grade opencode plugin with ClusterTentacle dynamic architecture. RGE + SRE = Execution Brain. Consolidated firewall. T1 precision context injectables.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --bundle --external @opencode-ai/plugin",
    "typecheck": "bun tsc --noEmit",
    "test": "bun test",
    "prepack": "bun run build"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.3.6"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.3.6",
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "opencode",
    "plugin",
    "agent",
    "multi-agent",
    "orchestrator",
    "shark",
    "manta",
    "kraken",
    "hive-mind",
    "cluster",
    "runtime-grade",
    "rge",
    "sre",
    "execution-brain",
    "firewall"
  ],
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.0.0"
  }
}
